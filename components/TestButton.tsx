interface TestButtonProps {
  variant?: any
  size?: any
}

export default function TestButton({ variant, size }: TestButtonProps) {
  return (
    <div className="">
      {/* TestButton component content */}
    </div>
  )
}